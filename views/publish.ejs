<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'/>
  <title>Publish</title>
  <%include res%></head>
<body>
  <div class="container publish">
    <!--NAV-->
    <%include nav.ejs %>
    <div class="ctrl-panel"> 
    <!--Article POOL-->
    <select multiple class="article">
      <%articles.forEach(function(a,i){
        %>
      <option value="<%=a._id%>"><%=a.title%>&nbsp;&nbsp;&nbsp;[<%=:a.createTime|dateFormat%>]</option>
      <%
      });%></select>
      <div class="ctrl">
        <button class="arr">=&gt;</button>
        <button class="arr">&lt;=</button>
      </div>
      <!--Article SELECTED-->
    <select multiple class="article">
      <%collection.forEach(function(a,i){
        %>
      <option value="<%=a._id%>"><%=a.title%></option>
      <%
      });%></select>
      <!--ALL TEMPLATES-->
      <div class="ctrl-form">
        <form action="/template/mail" method="get" accept-charset='utf-8'>
          <div>
            <label for="tid">Template</label>
            <select class="tpls" name="tid">
              <%tpls.forEach(function(a,i){%>          
              <option value="<%=a._id%>">
                <%=a.name%></option>
              <%});%></select>
          </div>
          <div>
            <label for="mailto">Email Add</label>
            <input type="email" name="mailto" id="mailto" required/>          
          </div>
          <input type="hidden" name="token" value="<%=token%>"/>
          <div>
            <button class="email" type='submit'>Email</button>
            <button class="render" type="button">Render</button>
          </div>
        </form>
      </div>
    </div>
    <iframe class="preview" src="javascript:;"></iframe>
    <%include foot%></div>
</body>
  <script type="text/javascript" src='//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js'></script>
  <script type="text/javascript" src="//netdna.bootstrapcdn.com/bootstrap/3.0.0/js/bootstrap.min.js"></script>
  <script src="/javascripts/bootbox.min.js"></script>
  <script type="text/javascript" src="/javascripts/utils.js?t=201308241007"></script>
  <script type="text/javascript" src="/javascripts/publish.js?t=201311122126"></script>
</html>